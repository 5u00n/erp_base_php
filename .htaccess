# "-d" must NOT be used here: "/" maps to DocumentRoot, which is a directory,
# so the rule would never run and you'd get 403 with "directory index" denied.

DirectoryIndex index.php

RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.php [L]
